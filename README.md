### Complete DS journey notes

setup commands:

pip install -r requirements.txt

pip install ipykernel 

for execution of jupter notebook

This notes is completely practised based on udemy course : "Complete DS, ML, DL, NLP Bootcamp"

# DS
